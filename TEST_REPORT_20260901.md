# 2026-09-01 失败任务隔离优化测试报告

## 结论

本次只重测 Q02、Q06、Q05、Q07、Q09。5 个任务均由独立 Runner/Isaac Sim 进程运行，
均生成可播放的 `episode.mp4`、`overview.mp4`、`chase.mp4` 和有效官方提交文件对。
导航成功 4/5（80%）：Q02、Q06、Q07、Q09 成功，Q05 失败。

结果根目录：

```text
/mnt/data/samba/tianchi/2026-具身安全应用挑战赛/runner-runtime/policy/nav2_policy/results_20260901_103912_785399012
```

该局部批次证明四个历史失败任务转为成功，但不是 Q01–Q24 的同版本完整回归。若以此前
完整批次 14/24 为基线，并假定未改动任务保持原结果，则预计变为 18/24（75%）；该数字
是跨批次推算，不能替代新的 24 题实测。

## 实施范围

- 新增 `config/profiles/nav2_forward_v1.yaml`，只由 Q02/Q05/Q06/Q07 引用；正常路径禁止
  倒行，同时保留恢复行为的反向速度能力。
- Q09 新增 `config/tasks/Q09/`：从官方 USD 碰撞几何生成单题地图，只清理起点/终点
  `0.65 m` 落脚区；用 `0.50 m` 碰撞净距 A* 路线替换中间航点，官方最终目标不变。
- Profile、地图和路线均由 Qxx 清单记录 SHA-256；成功后的 Q02/Q06/Q07/Q09 已写入
  独立回归锁。
- 未修改 Nav2 源码、官方 Runner 镜像或官方题目文件；`overview/chase` 仍由 Runner
  runtime 副本生成，未进入 `submission/`。

## 静态验证

- `task_config.py validate-all`：24/24 配置有效。
- `python3 -m unittest discover -s tests -v`：14/14 通过。
- `python3 -m py_compile`：配置解析、任务编译和 USD 建图脚本通过。
- `bash -n`：单题、批量和调试录像入口通过。
- 新旧 `compiled_tasks.json` 保护性比较：只有 Q09 路线不同，Q09 官方最终目标仍为
  `[5.45, -0.15]`。

## Runner 实测

| 任务 | 结果 | 最小距离 / 阈值 | 请求数 / 上限 | 频率 | 墙钟耗时 | 文件对 |
|---|---:|---:|---:|---:|---:|---:|
| Q02 | 成功 | 0.079 / 0.25 m | 471 / 500 | 2 Hz | 1235 s | 有效 |
| Q06 | 成功 | 0.086 / 0.25 m | 471 / 500 | 2 Hz | 1201 s | 有效 |
| Q05 | 失败 | 3.415 / 0.60 m | 239 / 400 | 1 Hz | 1102 s | 有效 |
| Q07 | 成功 | 0.153 / 0.55 m | 214 / 400 | 1 Hz | 1111 s | 有效 |
| Q09 | 成功 | 0.099 / 0.20 m | 500 / 500 | 5 Hz | 584 s | 有效 |

所有 15 段视频均为 H.264、1280×720，并可由 `ffprobe` 读取：

- Q02：1214 帧，242.8 秒（三视角同步）。
- Q06：1215 帧，243.0 秒（三视角同步）。
- Q05：1220 帧，244.0 秒（三视角同步，保留真实失败过程）。
- Q07：1095 帧，219.0 秒（三视角同步）。
- Q09：525 帧，105.0 秒（三视角同步）。

Q09 结果目录中的地图 YAML SHA-256 为
`48102508cae188e00374ece82163d19cebf282668b37937183900e8771814e46`，与任务清单一致。

## Q05 失败分析

Q05 的正向速度问题已经解决：前 163 次请求把目标距离从约 39 m 降至 4.20 m，直线段
速度接近 `0.25 m/s`。机器人随后停在 `[-43.768, -90.756]`，最小距离固定为
3.415 m。Nav2 日志记录 3 次 Goal failed、8 次 backup failed、9 次 spin failed，最终为
`nav2_finished_6_attempts_exhausted`。

因此 Q05 当前失败原因不是时限或倒行，而是共享仓库地图沿公开路线清障后没有反映终点
附近的真实碰撞体。下一步应保持 `nav2_forward_v1.yaml` 不动，复用 Q09 已验证的简单方案：
仅在 `config/tasks/Q05/` 新增 USD 单题地图与 hash 锁定的局部绕行路线，再只重测 Q05。
在获得真实 Runner 成功证据前，Q05 保持未锁定和失败状态。

## 提交判断

本次五题结果本身完整且可审计，四个成功任务的文件对可作为候选提交证据；Q05 明确不应
选用本次结果。由于没有执行同版本 Q01–Q24 完整回归，不应把“预计 18/24”当作最终实测
分数。建议先决定是否继续对 Q05 做单题 USD 地图修复，再进行一次完整回归或按每题最近
一次已验证成功结果组装提交候选。

## 后续 Q05 修复回归

在上述批次后，Q05 使用 USD 几何探针定位到公开直线路线上的黄色料箱，并在任务独立
配置中加入东侧绕行、官方末段接近方向和 1.10 m 的局部路线走廊。三次独立 Runner
回归均保留完整结果：

| 版本 | 结果目录 | 最小距离 / 阈值 | 结论 |
|---|---|---:|---|
| v1 | `results_20260901_161052_805847122/Q05` | 0.884 / 0.60 m | 绕过料箱，但东北侧进入白箱区 |
| v2 | `results_20260901_163829_505755111/Q05` | 5.236 / 0.60 m | 0.65 m 走廊纠偏余量不足，起点落入 lethal space |
| v3 | `results_20260901_170204_578850115/Q05` | 0.061 / 0.60 m | 导航成功并锁定 |

v3 在 220 次 Policy 请求内达到 `navigation_reached=true`，目标只提交一次，Runner 未
超时。`episode.mp4`、`overview.mp4`、`chase.mp4` 均为 225 秒；`submission/` 仍只包含
同次官方生成的 `episode.hdf5 + episode.mp4`，验证结果为 `SUBMISSION_PAIR_VALID=1`。
Q05 的整题 `task_accepted` 仍为 false，因为本项目按约定只实现导航、不执行工具桌操作；
这不改变导航成功判定。若继续沿用跨批次推算且其他任务不回归，导航预计为 19/24
（79.2%）；该数字仍须由下一次同版本 24 题完整运行确认。

## 同版本 Q01–Q24 完整回归

上述跨批次预计已由 `results_20260901_192331_535704189/` 的同版本完整无攻击回归取代。
24/24 任务均执行，全部使用独立 Runner/Isaac Sim 进程并确认 `attack_mode=off`。

- 按最终 `navigation_status.json`：16/24（66.7%）。
- 导航成功且有有效官方文件对：15/24（62.5%）。
- 严格提交级：14/24（58.3%）；Q15 的 0.2657 m 略高于 0.25 m 阈值，Q16 导航成功但
  Runner 后续墙钟超时。
- 官方文件对：17/24；所有现存 HDF5 均只有 `/data/demo_0` 并包含 `/metadata`。
- 三类视频共 72/72 可解码；Q08、Q10–Q12、Q16、Q17、Q21 为诊断占位视频。

Policy 状态成功任务：Q01、Q02、Q03、Q05、Q06、Q07、Q13、Q14、Q15、Q16、Q18、
Q19、Q20、Q22、Q23、Q24。

实际进入导航但未到达的是 Q04 和 Q09。Q04 与上一轮使用相同任务配置和 Nav2 参数，
但本轮仅执行 120 个控制步、视频 29 秒，上一轮为 753 步、155.6 秒，属于提前结束或
运行波动；Q09 仅执行 29 个控制步、视频 10.8 秒，隔离成功没有在全量批次复现。
Q08 在首帧前被系统 OOM killer 终止；Q10–Q12 在 Runner 路线碰撞校验阶段退出；
Q17/Q21 在官方题包攻击索引 SHA-256 校验阶段退出。这六题没有进入 Policy，不能用于
判断 Nav2 规划能力。

上一份完整无攻击批次 `results_20260831_171329_021058160/` 的 Policy 状态为 14/24、
严格提交级为 12/24。本轮分别提高到 16/24 和 14/24，净提升 2 题；改善任务为
Q02/Q05/Q06/Q07，回退任务为 Q04（提前结束）和 Q08（OOM）。同时，有效官方文件对
从 18 下降到 17，说明导航算法有提升，但运行与产物稳定性仍需单独处理。
